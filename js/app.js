// None of this actually does anything yet.
/*$(document).ready(function() {
	$('#button').click(function(event) {
		parseXML();
	})

	var parseXML = function() {
		var titleArr = []
		titleArr.append($('#inputBook1').val());
		titleArr.append($('#inputBook2').val());
		titleArr.append($('#inputBook3').val());
		console.log(titleArr);
		for (var i = 0; i < 3; i++) {
			$.get("http://www.librarything.com/api/thingTitle/" + titleArr[i], function(data) {
				
			})
		}
		
	}
});*/